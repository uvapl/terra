# Concerns

An app can pull in a concern to interface with another part of the system. For example, the file
tree concern creates a file tree component, makes sure it reports changes to the app, and at the
same time provide handlers that are copied into the app.
